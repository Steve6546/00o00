# 📋 SYSTEM OVERVIEW - Roblox Bot Project

> **تاريخ التحديث**: 2024-12-26  
> **الإصدار**: 3.0.0

---

## 📊 ملخص سريع

| الفئة | جاهز | يحتاج إعداد | غير مكتمل |
|-------|------|-------------|-----------|
| **الأنظمة الأساسية** | 6 | 0 | 0 |
| **الأنظمة الخارجية** | 0 | 5 | 0 |
| **الأدوات المساعدة** | 4 | 0 | 0 |

---

## 🟢 الأنظمة الجاهزة للاستخدام

### 1. نظام Shell التفاعلي ✅
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `bot.py` | Shell احترافي مع دعم AR/EN | ✅ جاهز |
| `shell.py` | Shell أساسي | ✅ جاهز |
| `shell_v2.py` | Shell مع auto-complete | ✅ جاهز |
| `cli.py` | واجهة سطر الأوامر Click | ✅ جاهز |

**طريقة التشغيل:**
```bash
.venv\Scripts\python.exe bot.py --lang ar
```

---

### 2. نظام قاعدة البيانات ✅
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `data/database.py` | إدارة قاعدة البيانات SQLite | ✅ جاهز |
| `data/bot.db` | ملف قاعدة البيانات | ✅ موجود |

**الجداول:**
- `Account` - الحسابات المُنشأة
- `Proxy` - قائمة البروكسي
- `TaskLog` - سجل المهام
- `FollowRecord` - سجل المتابعات

---

### 3. نظام التفتيش (Inspector) ✅
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/control/inspector.py` | تفتيش الحسابات والنظام | ✅ جاهز |

**الوظائف:**
- `inspect_account()` - تفتيش حساب واحد
- `inspect_all_accounts()` - تفتيش جميع الحسابات
- `inspect_proxies()` - تفتيش البروكسيات
- `inspect_system()` - فحص شامل للنظام

---

### 4. نظام مولد الهوية ✅
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/generators/identity_generator.py` | توليد أسماء/كلمات مرور | ✅ جاهز |

**الميزات:**
- توليد أسماء مستخدمين عشوائية
- توليد كلمات مرور آمنة
- توليد تواريخ ميلاد
- دعم الجنس (ذكر/أنثى)

---

### 5. نظام السلوك البشري ✅
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/behavior/human_input.py` | محاكاة الكتابة البشرية | ✅ جاهز |
| `src/behavior/interaction.py` | تفاعلات بشرية | ✅ جاهز |
| `src/behavior/sticky.py` | سلوكيات ثابتة | ✅ جاهز |

---

### 6. نظام الـ Flows ✅
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/flows/account_flow.py` | إنشاء الحسابات | ✅ جاهز |
| `src/flows/follow_flow.py` | متابعة المستخدمين | ✅ جاهز |
| `src/flows/enhanced_account_flow.py` | إنشاء محسن | ✅ جاهز |

---

## 🟡 أنظمة تحتاج إعداد خارجي

### 7. نظام حل الـ CAPTCHA ⚙️
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/modules/captcha_solver.py` | حلول CAPTCHA متعددة | ⚙️ يحتاج API Key |
| `scripts/nopecha_solver.py` | NopeCHA integration | ⚙️ يحتاج API Key |
| `scripts/free_captcha.py` | حلول مجانية | ⚙️ تجريبي |

**الخدمات المدعومة:**
| الخدمة | النوع | الإعداد المطلوب |
|--------|-------|-----------------|
| 2Captcha | مدفوع | `CAPTCHA_API_KEY` في البيئة |
| CapMonster | مدفوع | `CAPTCHA_API_KEY` في البيئة |
| NopeCHA | مجاني/مدفوع | `NOPECHA_KEY` في البيئة |
| ReloadSolver | مجاني | لا يحتاج (يعيد تحميل الصفحة) |

**إعدادات في `config/config.yaml`:**
```yaml
captcha:
  service: none  # 2captcha, capmonster, nopecha
  api_key: ""    # أو عبر CAPTCHA_API_KEY
```

---

### 8. نظام البروكسي ⚙️
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/core/proxy_manager.py` | إدارة البروكسيات | ⚙️ يحتاج بروكسيات |
| `scripts/auto_proxy.py` | جلب بروكسيات مجانية | ⚙️ غير موثوق |
| `scripts/mubeng_integration.py` | تدوير البروكسي | ⚙️ يحتاج Mubeng |

**الإعداد في `config/config.yaml`:**
```yaml
proxy:
  enabled: false      # تفعيل البروكسي
  auto_fetch: true    # جلب بروكسيات مجانية
mubeng:
  enabled: false      # تدوير البروكسي
  port: 8080
```

---

### 9. نظام البريد الإلكتروني ⚙️
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/services/email_service.py` | خدمة البريد IMAP | ⚙️ يحتاج سيرفر |
| `src/services/free_email.py` | بريد مجاني مؤقت | ⚙️ تجريبي |

**المتطلبات:**
1. دومين خاص ($10/سنة)
2. سيرفر بريد (Maddy موصى به)
3. إعدادات DNS

**الإعداد في `config/config.yaml`:**
```yaml
email:
  enabled: false
  imap_server: ""      # mail.yourdomain.com
  domain: ""           # yourdomain.com
  username: ""
  password: ""         # أو EMAIL_PASSWORD
```

---

### 10. نظام الجدولة (Scheduler) ⚙️
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/control/scheduler.py` | جدولة المهام | ⚙️ غير مفعل |

**الميزات:**
- إنشاء حسابات تلقائي
- متابعة مجدولة
- تشغيل في أوقات محددة

**الإعداد في `config/config.yaml`:**
```yaml
schedule:
  enabled: false
  account_creation:
    enabled: false
    count: 2
    interval_hours: 6
```

---

### 11. قواعد التحسين الذاتي (Rules Engine) ⚙️
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/control/commander.py` | محرك القواعد | ⚙️ مدمج |

**القواعد:**
- زيادة التأخير بعد الفشل المتكرر
- إيقاف مؤقت عند اكتشاف حظر
- تعديل بناءً على نسبة النجاح

---

## 🔧 الأدوات المساعدة

### 12. نظام الـ Core
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/core/state_machine.py` | آلة الحالات | ✅ جاهز |
| `src/core/page_detector.py` | اكتشاف الصفحات | ✅ جاهز |
| `src/core/session_manager.py` | إدارة الجلسات | ✅ جاهز |
| `src/core/stealth_layer.py` | طبقة التخفي | ✅ جاهز |

### 13. نظام الخدمات
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/services/anti_detection.py` | مكافحة الاكتشاف | ✅ جاهز |
| `src/services/health_checker.py` | فحص صحة الحسابات | ✅ جاهز |

### 14. نظام الوحدات
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `src/modules/account_creator.py` | إنشاء الحسابات | ✅ جاهز |
| `src/modules/follow_bot.py` | بوت المتابعة | ✅ جاهز |
| `src/modules/runner.py` | تشغيل المهام | ✅ جاهز |

---

## 📁 هيكل المشروع

```
00o00/
├── bot.py              ⭐ Shell الرئيسي (ابدأ من هنا)
├── cli.py              CLI بـ Click
├── shell.py            Shell أساسي
├── shell_v2.py         Shell مع auto-complete
├── main.py             نقطة دخول قديمة
│
├── config/
│   └── config.yaml     ⚙️ جميع الإعدادات هنا
│
├── data/
│   ├── database.py     إدارة البيانات
│   └── bot.db          قاعدة البيانات
│
├── src/
│   ├── core/           النواة (state machine, proxy, session)
│   ├── flows/          الـ flows (account, follow)
│   ├── services/       الخدمات (email, health, anti-detection)
│   ├── generators/     المولدات (identity)
│   ├── behavior/       السلوك البشري
│   ├── control/        التحكم (commander, inspector, scheduler)
│   └── modules/        الوحدات (captcha, account, follow)
│
├── scripts/            سكربتات مساعدة
├── docs/               التوثيق
├── logs/               سجلات التشغيل
├── sessions/           جلسات المتصفح
└── tests/              الاختبارات
```

---

## 🚀 البدء السريع

### 1. تشغيل الـ Shell
```bash
.venv\Scripts\python.exe bot.py
```

### 2. الأوامر الأساسية
```
bot> /              # عرض جميع الأوامر
bot> status         # حالة النظام
bot> accounts       # إدارة الحسابات
bot> lang ar        # تحويل للعربية
bot> exit           # خروج
```

---

## ⚠️ ملاحظات مهمة

1. **الأنظمة المدفوعة**: CAPTCHA solvers تحتاج اشتراك
2. **البريد**: يحتاج دومين وسيرفر خاص
3. **البروكسي**: المجاني غير موثوق، يُفضل المدفوع
4. **الجدولة**: معطلة افتراضياً لأسباب أمان

---

## 📞 للمساعدة

- اقرأ `docs/troubleshooting.md` لحل المشاكل
- اقرأ `docs/architecture.md` لفهم البنية
- افتح `config/config.yaml` لتعديل الإعدادات
